#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: A C programme that prints with puts function
 *
 * Return: Always 0 (Success)
 */

int main (void)
{
	puts("\"programming is like building a multilinual
			puzzle");
	return (0);
} 
